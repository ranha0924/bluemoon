# CLAUDE.md

## 작업 절차 (필수)

이 저장소에서 작업할 때는 아래 3단계를 반드시 지킨다.

### 1. 구현 전에 플랜부터

코드를 건드리기 전에 **Plan 에이전트**(`Agent` 도구, `subagent_type: "Plan"`)로 구현 계획을
먼저 세운다. 계획에는 수정할 파일, 변경 순서, 예상 위험 요소가 들어가야 한다.
계획을 사용자에게 보여주고 진행한다.

오타 수정, 문구 변경 같은 한 줄짜리 기계적 작업은 예외로 둔다.

### 2. 작업 단위마다 검증

작업을 여러 단계로 쪼갠 뒤, **한 단계가 끝날 때마다 검증하고 그 결과를 사용자에게 보고한 다음**
다음 단계로 넘어간다. 여러 단계를 몰아서 하고 마지막에 한 번만 확인하지 않는다.

검증은 "코드를 읽어 봤다"가 아니라 **실제로 실행한 결과**여야 한다. 브라우저에서 렌더링해
확인하거나, 수치를 측정하거나, 스크린샷을 남긴다. 실패했으면 실패한 그대로 보고한다.

### 3. 마지막에 독립 에이전트로 최종 검증

작업을 마무리하기 전에 **반드시 독립된 에이전트**(`Agent` 도구)를 띄워 최종 검증을 받는다.
구현한 주체와 검증하는 주체를 분리하는 것이 핵심이므로, 다음을 지킨다.

- 검증 에이전트에게 **"문제가 없다"는 결론을 유도하지 않는다.** 무엇을 만들었는지와
  어디를 봐야 하는지만 알려주고, 판단은 에이전트에게 맡긴다.
- 에이전트가 지적한 내용을 그대로 받아들이지 않는다. **직접 재현해 보고** 사실인 것만 고친다.
  틀린 지적이면 왜 틀렸는지 근거를 들어 사용자에게 설명한다.
- 최종 검증을 통과하지 못했으면 완료라고 보고하지 않는다.

---

## 프로젝트 개요

`index.html` 한 파일에 전부 들어 있는 한국어 **가스안전 교육 추리 어드벤처**.
실제 가스안전 수칙·통계를 단서와 추리 문항으로 옮긴 공모전 출품작이며, 빌드 도구도 외부 의존성도
없다. 자세한 구조는 `README.md` 참고.

주의할 점:

- **`#game`은 1000×625 고정 좌표계다.** 표시 크기는 `transform:scale()`로만 바뀐다
  (`fitStage()`가 `--s` 갱신). 그래서 화면 크기별 미디어 쿼리 분기를 새로 추가하면 안 된다 —
  프레임과 내용이 같은 비율로 줄어드는 전제가 깨진다.
- **이미지는 `IMG`(로컬 `assets/*.webp`) → `CDN`(원본 PNG) 순으로 폴백한다.**
  이미지를 추가할 때는 두 객체에 같은 key를 넣고, 화면에 붙는 `<img>`에는 `data-k`를 준다.
- 효과음은 WebAudio로 직접 합성한다. 사운드 파일을 추가하지 않는다.

## 검증 방법

이 환경에는 Chromium이 미리 깔려 있다. `playwright install`을 실행하지 말고 아래 경로를 쓴다.

```
/opt/pw-browsers/chromium-1194/chrome-linux/chrome
```

`playwright`, `Pillow`는 세션마다 `pip install`이 필요할 수 있다.
로컬 서버로 띄워서 검사한다 (`file://`는 폴백 동작이 달라진다).

```bash
python3 -m http.server 8899   # 그 뒤 http://localhost:8899/index.html
```

UI를 수정했다면 최소한 아래를 확인한다.

1. **전체 플레이 통과** — 프롤로그 → 단서 8개 → 증언 `t1` → 추리 4문항 → 엔딩.
   `G.clues`, `G.testis` 같은 내부 상태를 `page.evaluate`로 직접 읽어 확인한다.
2. **이미지 23장 전부 200**, 그리고 `naturalWidth === 0`인 `<img>`가 없어야 한다.
3. **콘솔 에러 0건.**
4. **레이아웃 겹침** — 여러 화면 크기(1100×760 ~ 420×760)에서 `#toast`와 화면 내용의
   바운딩 박스가 교차하지 않고, 패널이 프레임 밖으로 나가지 않아야 한다.
   눈으로 보고 넘기지 말고 좌표를 계산해서 검사한다.

`.screen`은 `display:none`으로 숨기기만 하고 DOM에서 제거하지 않는다. 그래서 **모든 화면의
버튼이 항상 동시에 존재한다.** `class="btn"`은 8개 컨테이너(`scr-title` / `scr-lobby` /
`scr-hub`×4 / `scr-room` / `scr-talk` / `scr-end`×2 / `scr-inspect` / `#notebook`)에
12개가 깔려 있다. 사건별 `backLabel`(`"캠핑장으로"` / `"펜션으로"`)은 `scr-room`에만 적용되고,
`scr-talk`의 `"인물 선택으로"`(인물 선택창 복귀)와 `scr-inspect`의 `"돌아가기"`는 정적
텍스트다. 반드시 `#scr-room .btn`처럼 화면 단위로 범위를 좁혀서 잡는다.

`.spot-btn`(`#spots`)과 `.q-btn`(`#talkQs`)은 각각 한 화면에만 생성되므로 이 문제가 없다.

## Git

- 작업 브랜치: `claude/kgs-ai-contest-redesign-y3zxg1`
- **푸시 전에 `git branch --show-current`로 현재 브랜치를 확인한다.** 머지 직후에는 `main`이
  체크아웃된 상태로 남아 있을 수 있어서, 확인 없이 밀면 `main`으로 바로 들어간다.
- 푸시는 `git push -u origin <branch>`. 네트워크 실패 시에만 2s/4s/8s/16s 백오프로 재시도.
- PR은 사용자가 명시적으로 요청할 때만 만든다.
